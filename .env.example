# Backend (local host run)
PORT=3001
DATABASE_URL=postgres://postgres:postgres@localhost:5432/wpar
REDIS_URL=redis://localhost:6379
OPENROUTER_API_KEY=changeme
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1

# Pairing + signing
PAIRING_BOOTSTRAP_SECRET=change-this-shared-bootstrap-secret
SIGNATURE_TTL_SECONDS=180
SIGNATURE_MAX_SKEW_SECONDS=300
# 64-byte Ed25519 secret key (base64). Local dev sample key:
BACKEND_SIGNING_PRIVATE_KEY=tymBbZJonEa5diaN8AdqxQB8r3n0kbyH8LfSExagF+QGDUymnMJ37gDXKwFlrdwC8e3LMvOOgUZKLK9i4tnlfw==
BACKEND_SIGNING_AUDIENCE=wp-agent-runtime
BACKEND_PUBLIC_BASE_URL=http://localhost:3001

# Backend (when running inside docker compose)
DATABASE_URL_DOCKER=postgres://postgres:postgres@backend-db:5432/wpar
REDIS_URL_DOCKER=redis://redis:6379

# WP dev target
WP_SITE_URL=http://localhost:8080
WP_TOOL_API_BASE=http://localhost:8080/wp-json/wp-agent/v1
WP_AGENT_BACKEND_BASE_URL=http://localhost:3001
WP_AGENT_BACKEND_AUDIENCE=wp-agent-runtime

# WP docker database (for wordpress container)
WP_DB_HOST=localhost:3306
WP_DB_NAME=wp_agent
WP_DB_USER=wp_agent
WP_DB_PASSWORD=wp_agent

# WP tool API security defaults
WP_AGENT_RATE_LIMIT_WINDOW_SECONDS=60
WP_AGENT_RATE_LIMIT_REQUESTS_PER_WINDOW=60

# Chat policy/runtime defaults (priority-ordered fallbacks)
CHAT_MODEL_FAST=openai:gpt-5-mini
CHAT_MODEL_BALANCED=openai:gpt-5.1,openai:gpt-5
CHAT_MODEL_QUALITY=openai:gpt-5.2-pro,openai:gpt-5.2,openai:gpt-5
CHAT_MODEL_REASONING=openai:gpt-5.2

# Limits (these are app-policy, not model-policy)
CHAT_RATE_LIMIT_PER_MINUTE=20
CHAT_DAILY_TOKEN_CAP=50000
CHAT_MAX_PROMPT_MESSAGES=12
CHAT_MAX_INPUT_CHARS=4000
CHAT_SESSION_RETENTION_DAYS=30

# M3 skills + plans defaults
SKILL_SOURCE_REPO_URL=
SKILL_SOURCE_COMMIT_SHA=
PLAN_MAX_STEPS=12
PLAN_MAX_TOOL_CALLS=40
PLAN_MAX_PAGES=200
PLAN_MAX_COST_USD=5
