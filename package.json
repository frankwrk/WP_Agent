{
  "name": "wp-agent-runtime",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "workspaces": [
    "apps/backend",
    "apps/wp-plugin/admin",
    "packages/*"
  ],
  "scripts": {
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "dev": "pnpm -r dev",
    "fmt": "pnpm -r fmt"
  }
}
